import { DefaultTheme } from 'react-native-paper';

export const theme = {
  ...DefaultTheme,
  colors: {
    ...DefaultTheme.colors,
    grey: "#969696",
    white: "#FFF",
    orange: "#B87F3D",
    black: "#000"
  },
};